{% extends 'back.html.twig' %}
{% block css %}
{{ parent() }}
{%endblock%}

{% block title %}Commande detail{% endblock %}
{% block navbar %}
{{ parent() }}
{%endblock%}

{% block body %}
{{ parent() }}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commande.date|date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ commande.statut }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ commande.total }}</td>
            </tr>
            <tr>
                <th>Type_paiement</th>
                <td>{{ commande.typePaiement }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-inverse-primary btn-fw"  href="{{ path('app_commande_index') }}">back to list<i class="mdi mdi-reply"></i></a>

    <a class="btn btn-inverse-info btn-fw" href="{{ path('app_commande_edit', {'id': commande.id}) }}">edit<i class="mdi mdi-border-color"></i> </a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
