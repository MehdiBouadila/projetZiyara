<form method="post" action="{{ path('app_code_delete', {'id': code.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code.id) }}">
    <button class="btn btn-inverse-danger btn-fw">Delete<i class="mdi mdi-delete"></i></button>
</form>
