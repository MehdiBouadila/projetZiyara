{% extends 'back.html.twig' %}
{% block css %}
{{ parent() }}
{%endblock%}

{% block title %}code promo detail{% endblock %}
{% block navbar %}
{{ parent() }}
{%endblock%}

{% block body %}
{{ parent() }}
    <h1>Code</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ code.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ code.code }}</td>
            </tr>
            <tr>
                <th>Reduction</th>
                <td>{{ code.reduction }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ code.dateDebut }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ code.dateFin }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-inverse-primary btn-fw"  href="{{ path('app_code_index') }}">back to list<i class="mdi mdi-reply"></i></a>

    <a class="btn btn-inverse-info btn-fw" href="{{ path('app_code_edit', {'id': code.id}) }}">edit<i class="mdi mdi-border-color"></i> </a>

    {{ include('code/_delete_form.html.twig') }}
{% endblock %}
