{% extends 'back.html.twig' %}

{% block css %}
 {{ parent() }} 
{% endblock %} 

{% block title %}Edit Transport{% endblock %}

{% block body %}
{{ parent() }}
    <h1>Edit Transport</h1>

    {{ include('back/transport/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_transport_index') }}" class="btn btn-primary">back to list</a>

    {{ include('/back/transport/_delete_form.html.twig') }}
{% endblock %}
