<form method="post" action="{{ path('app_categorie_visite_delete', {'id': categorie_visite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_visite.id) }}">
    <button class="form-btn">Delete</button>
</form>
