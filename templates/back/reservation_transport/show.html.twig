{% extends 'back.html.twig' %}

{% block css %}
 {{ parent() }} 
{% endblock %}

{% block title %}ReservationTransport{% endblock %}

{% block body %}

{{ parent() }}
    <h1>ReservationTransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_transport.id }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ reservation_transport.idUser }}</td>
            </tr>
            <tr>
                <th>IdTransport</th>
                <td>{{ reservation_transport.idTransport }}</td>
            </tr>
            <tr>
                <th>DateReservationTransport</th>
                <td>{{ reservation_transport.dateReservationTransport ? reservation_transport.dateReservationTransport|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PointDepart</th>
                <td>{{ reservation_transport.pointDepart }}</td>
            </tr>
            <tr>
                <th>PointArrive</th>
                <td>{{ reservation_transport.pointArrive }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_transport_index') }}"class="btn btn-primary">back to list</a>

    <a href="{{ path('app_reservation_transport_edit', {'id': reservation_transport.id}) }}"class="btn btn-info">edit</a>

    {{ include('/back/reservation_transport/_delete_form.html.twig') }}
{% endblock %}
