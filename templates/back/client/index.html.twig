{% extends 'back.html.twig' %}

{% block css %}
 {{ parent() }} 
{% endblock %}


{% block title %}Client index{% endblock %}


{% block navbar %}
 {{ parent() }} 
{% endblock %}

{% block body %}

 {{ parent() }}
    <h1>Client index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdClient</th>
                <th>NomClient</th>
                <th>PrenomClient</th>
                <th>EmailClient</th>
                <th>MdpClient</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for client in clients %}
            <tr>
                <td>{{ client.idClient }}</td>
                <td>{{ client.nomClient }}</td>
                <td>{{ client.prenomClient }}</td>
                <td>{{ client.emailClient }}</td>
                <td>{{ client.mdpClient }}</td>
                <td>
           <div class="mt-3"> <!-- Add margin top for spacing -->
    <a href="{{ path('app_client_show', {'idClient': client.idClient}) }}" class="btn btn-primary">Show</a>
</div>

<div class="mt-3"> <!-- Add margin top for spacing -->
    <a href="{{ path('app_client_edit', {'idClient': client.idClient}) }}" class="btn btn-info">Edit</a>
</div>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="mt-3"> <!-- Add margin top for spacing -->
      <a href="{{ path('app_client_new') }}" class="btn btn-primary">Create New</a>
    </div>




{% endblock %}
