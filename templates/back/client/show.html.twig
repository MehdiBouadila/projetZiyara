
{% extends 'back.html.twig' %}

{% block css %}
 {{ parent() }} 
{% endblock %}

{% block body %}
    <!DOCTYPE html>
    <html lang="en">


        {{ parent() }}

        <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdClient</th>
                <td>{{ client.idClient }}</td>
            </tr>
            <tr>
                <th>NomClient</th>
                <td>{{ client.nomClient }}</td>
            </tr>
            <tr>
                <th>PrenomClient</th>
                <td>{{ client.prenomClient }}</td>
            </tr>
            <tr>
                <th>EmailClient</th>
                <td>{{ client.emailClient }}</td>
            </tr>
            <tr>
                <th>MdpClient</th>
                <td>{{ client.mdpClient }}</td>
            </tr>
        </tbody>
    </table>

<div class="mt-3"> <!-- Add margin top for spacing -->
    <a href="{{ path('app_client_index') }}" class="btn btn-secondary">Back to List</a>
</div>

<div class="mt-3"> <!-- Add margin top for spacing -->
    <a href="{{ path('app_client_edit', {'idClient': client.idClient}) }}" class="btn btn-info">Edit</a>
</div>

    {{ include('back/client/_delete_form.html.twig') }}
    </html>

{% endblock %}

 {#{% block footer %}
  {{ parent() }}
 {% endblock %}#}