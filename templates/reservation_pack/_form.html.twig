{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

<div class="form-group row">
	<label for="{{ form.dateReservationPack.vars.id }}" class="col-sm-2 col-form-label">Reservation Date</label>
	<div class="col-sm-10">
		{{ form_widget(form.dateReservationPack, {'attr': {'class': 'form-control', 'placeholder': 'Reservation Date'}}) }}
		<div class="invalid-feedback">
			{{ form_errors(form.dateReservationPack) }}
		</div>
	</div>
</div>

<div class="form-group row">
	<label for="{{ form.nbrParticipantPack.vars.id }}" class="col-sm-2 col-form-label">Number of Participants</label>
	<div class="col-sm-10">
		{{ form_widget(form.nbrParticipantPack, {'attr': {'class': 'form-control', 'placeholder': 'Number of Participants'}}) }}
		<div class="invalid-feedback">
			{{ form_errors(form.nbrParticipantPack) }}
		</div>
	</div>
</div>

<div class="form-group row">
	<label for="{{ form.pack.vars.id }}" class="col-sm-2 col-form-label">Pack</label>
	<div class="col-sm-10">
		{{ form_widget(form.pack, {'attr': {'class': 'form-control'}}) }}
		<div class="invalid-feedback">
			{{ form_errors(form.pack) }}
		</div>
	</div>
</div>

<div class="form-group row">
	<label for="{{ form.user.vars.id }}" class="col-sm-2 col-form-label">User</label>
	<div class="col-sm-10">
		{{ form_widget(form.user, {'attr': {'class': 'form-control'}}) }}
		<div class="invalid-feedback">
			{{ form_errors(form.user) }}
		</div>
	</div>
</div>

<button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
